#!/bin/bash

set -o nounset
set -o errexit

DMENU="dmenu -l 2 -c -bw 2 -i" 

chosen=$(echo -e "Dvorak\nUS" | $DMENU -p "Select layout")

case "$chosen" in
  Dvorak) 	setxkbmap dvorak ;;
  US) 		setxkbmap us ;;
esac

