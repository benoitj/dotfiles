#!/bin/sh

set -e

currentBranch="$(git rev-parse --abbrev-ref HEAD)"
countOfPushes="$(git rev-list HEAD...origin/"""$currentBranch""" --ignore-submodules --count 2>/dev/null)"

test -n "$countOfPushes" -a "$countOfPushes" -gt 0 || exit 1
